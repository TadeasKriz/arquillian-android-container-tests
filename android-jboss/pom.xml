<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Parent -->
    <parent>
        <groupId>net.miklosovic.android.container.tests</groupId>
        <artifactId>android-container-tests-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>android-jboss</artifactId>
    <packaging>jar</packaging>
    <name>Arquillian Android Container + JBoss AS Test</name>

    <properties>
        <version.android.container.depchain>0.0.1-SNAPSHOT</version.android.container.depchain>
        <version.android.drone.web>0.0.1-SNAPSHOT</version.android.drone.web>
        <version.org.jboss.drone.bom>1.2.0.Final-SNAPSHOT</version.org.jboss.drone.bom>
        <version.org.arquillian.bom>1.0.3.Final</version.org.arquillian.bom>
        <version.jboss.container.managed>7.1.1.Final</version.jboss.container.managed>
        <version.org.jboss.arquillian.graphene2>2.0.0.Alpha3</version.org.jboss.arquillian.graphene2>
    </properties>

    <dependencyManagement>
        <dependencies>
             <!-- Arquillian BOM -->
            <dependency>
                <groupId>org.jboss.arquillian</groupId>
                <artifactId>arquillian-bom</artifactId>
                <version>${version.org.arquillian.bom}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>org.jboss.arquillian.graphene</groupId>
                <artifactId>graphene-webdriver</artifactId>
                <version>${version.org.jboss.arquillian.graphene2}</version>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
       <!-- Java EE6 stack -->
        <dependency>
            <groupId>org.jboss.spec</groupId>
            <artifactId>jboss-javaee-web-6.0</artifactId>
            <version>3.0.1.Final</version>
            <type>pom</type>
            <scope>provided</scope>
            <exclusions>
                <exclusion>
                    <groupId>xalan</groupId>
                    <artifactId>xalan</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Arquillian and JUnit -->
        <dependency>
            <groupId>org.jboss.arquillian.junit</groupId>
            <artifactId>arquillian-junit-container</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Graphene Webdriver -->
        <dependency>
            <groupId>org.jboss.arquillian.graphene</groupId>
            <artifactId>graphene-webdriver</artifactId>
            <type>pom</type>
            <scope>test</scope>
        </dependency>

        <!-- JUnit -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
            <version>4.8.2</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.3.2</version>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.12.3</version>
            </plugin>
            <plugin>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.2</version>
                <configuration>
                    <!-- Java EE 6 does not require web.xml -->
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>process-test-resources</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>**/arquillian.xml</include>
                </includes>
            </testResource>
        </testResources>
    </build>

    <profiles>
        <!-- Multiple containers -->
        <profile>
            <id>android-jboss</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>

            <properties>
                <jboss.home>/home/smikloso/devel/servers/jboss-as-7.1.1.Final</jboss.home>
                <android.avd.name>android-managed-1</android.avd.name>
                <android.sd.size>128M</android.sd.size>
                <android.emulator.bootup.timeout>600</android.emulator.bootup.timeout>
                <android.api.level>16</android.api.level>
                <android.abi>armeabi-v7a</android.abi>
                <webdriver.remote.address>http://localhost:14444/wd/hub</webdriver.remote.address>
                <webdriver.browsercapabilities>android</webdriver.browsercapabilities>
                <arquillian.drone.verbose>true</arquillian.drone.verbose>
                <arquillian.drone.apkserver>src/test/resources/servers/android-server-2.16.apk</arquillian.drone.apkserver>
            </properties>

            <dependencies>
                <!-- JBoss AS 7 managed -->
                <dependency>
                    <groupId>org.jboss.as</groupId>
                    <artifactId>jboss-as-arquillian-container-managed</artifactId>
                    <version>${version.jboss.container.managed}</version>
                    <scope>test</scope>
                </dependency>

                <!-- Android Container Depchain -->
                <dependency>
                    <groupId>org.jboss.arquillian.container</groupId>
                    <artifactId>android-container-depchain</artifactId>
                    <type>pom</type>
                    <scope>test</scope>
                    <version>${project.version}</version>
                </dependency>

                <!-- Android Drone for web testing -->
                <dependency>
                    <groupId>org.jboss.arquillian.extension</groupId>
                    <artifactId>arquillian-container-android-drone-web</artifactId>
                    <version>${version.android.drone.web}</version>
                    <scope>test</scope>
                </dependency>

                <!-- Use Servlet Protocol with Arquillian -->
                <dependency>
                    <groupId>org.jboss.arquillian.protocol</groupId>
                    <artifactId>arquillian-protocol-servlet</artifactId>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>
</project> 
